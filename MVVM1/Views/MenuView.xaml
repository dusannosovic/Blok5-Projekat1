<UserControl x:Class="MVVM1.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM1.Views"
             xmlns:vml="clr-namespace:MVVM1.VML"
             xmlns:viewModel="clr-namespace:MVVM1.ViewModel"
             vml:ViewModelLocator.AutoHookedUpViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.DataContext>
        <viewModel:MenuViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModel:AccountDetailsViewModel}">
            <local:AccountDetailsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:StudentViewModel}">
            <local:StudentView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:PictureViewModel}">
            <local:PictureView/>
        </DataTemplate>
        <Style x:Key="myLBStyle" TargetType="{x:Type MenuItem}">
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                             Color="Transparent"/>
            </Style.Resources>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="5">
            <Menu DockPanel.Dock="Top" Name="Menu1" ItemContainerStyle="{StaticResource myLBStyle}">
                <MenuItem Header="Home" Command="{Binding NavCommand}" Name="MenuItem1"
                          CommandParameter="picView"/>
                <MenuItem Header="List View" Command="{Binding NavCommand}" Name="MenuItem2"
                          CommandParameter="addPic"/>
                <MenuItem Header="Account Details" Command="{Binding NavCommand}" CommandParameter="accountDet" Name="MenuItem3"/>
            </Menu>
        </DockPanel>
        <Grid Grid.Row="1">
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Grid>
    </Grid>
</UserControl>
